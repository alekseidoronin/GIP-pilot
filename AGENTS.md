# AGENTS.md

## Cursor Cloud specific instructions

This is currently an empty repository (single `README.md`). There are no services, dependencies, or build steps.

When application code is added, update this section with:
- How to install dependencies (package manager, lockfile type)
- How to run lint, tests, and the dev server
- Any non-obvious environment setup caveats
